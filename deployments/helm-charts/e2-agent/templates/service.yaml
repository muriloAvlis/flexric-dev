apiVersion: v1
kind: Service
metadata:
  name: {{ include "e2-agent.fullname" . }}
  labels:
    {{- include "e2-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "e2-agent.selectorLabels" . | nindent 4 }}
